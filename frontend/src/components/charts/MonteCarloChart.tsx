"use client";

export default function MonteCarloChart() {
  return (
    <div className="w-full h-64 flex items-center justify-center bg-slate-100 rounded">
      <span className="text-slate-400">MonteCarloChart — render with Recharts</span>
    </div>
  );
}
